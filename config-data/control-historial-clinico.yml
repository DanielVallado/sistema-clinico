server:
  port: {PORT:${SERVER_PORT:0}}
  servlet:
    context-path: /control-historial-clinico

spring:
  application:
    name: control-historial-clinico
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/control_historial_clinico
    username: root
    password: TamiLala#_7
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  cloud:
    discovery:
      enabled: true
  eureka:
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        default-zone: http://localhost:8071/eureka
      instance:
        instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
